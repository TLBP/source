<?xml version="1.0" encoding="UTF-8"?>
<!-- ********************************************************************
     $Id: part4.xml,v 1.12 2002/12/20 23:23:58 nilgun Exp $
     ********************************************************************-->
<part xml:id="bashref_shell.builtins">
  <title>Yerleşik Kabuk Komutları</title>
  <titleabbrev>Kabuğun birer parçası olan komutlar.</titleabbrev>
  <partintro>
    <para>
Yerleşik komutlar kabuğun kendi içinde bulunur. Bir yerleşik komutun ismi bir <link linkend="bashref_shell.commands-simple">basit komutun</link> ilk sözcüğü olarak kullanılmışsa, kabuk başka bir uygulamayı başlatmaksızın komutu doğrudan doğruya çalıştırır. Yerleşik komutlar ayrı uygulamalar olarak çalıştırılmaları halinde sorunlar çıkarabilecek ya da ayrı bir uygulama olarak çalıştırılması imkansız işlevselliği sağlamak için gereklidir.
    </para><para>
Bu oylumda Bourne Kabuğundan alıntılanan yerleşikler ile Bash'ın kendisine özgü ya da sonradan eklenmiş yerleşik komutlarından kısaca bahsedilecektir.
    </para><para>
Bu oylumda yer verilmemiş yerleşik komutlar başka bölümlerde ele alınmıştır: İş denetimine Bash arayüzü sağlayanlar <xref linkend="bashref_job.control.builtins"/> bölümünde, dizin yığını <xref linkend="bashref_directory-builtins"/> bölümünde, komut geçmişi <xref linkend="bashref_bash.history.builtins"/> bölümünde, ve programlanabilir tamamlama <xref linkend="bashref_programmable.builtins"/> bölümünde anlatılmıştır.
    </para><para>
Yerleşiklerin çoğu POSIX ya da Bash tarafından eklenmiştir.
    </para><para>
Aksi belirtilmedikçe, <command>-</command> ile öncelenmiş seçenekler kabul eden belgelenmiş her yerleşik komut seçeneklerin sonunu işaretlemek için <command>-</command> kabul eder. Örneğin, <command>:</command>, <command>true</command>, <command>false</command> ve <command>test</command> yerleşikleri seçenek kabul etmezler.
  </para></partintro>

<chapter xml:id="bashref_bourne.shell.builtins" label="1">
  <title>Bourne Kabuğu Yerleşikleri</title>
  <titleabbrev>Bourne Kabuğundan miras alınan yerleşik komutlar.</titleabbrev>
<indexterm scope="bashref_concept.index">
<primary>yerleşik komutlar</primary><secondary>Bourne kabuğu</secondary></indexterm>
  <para>
Aşağıdaki kabuk yerleşik komutları Bourne Kabuğundan miras alınmıştır. Bu komutlar POSIX standardında belirtildiği gibi gerçeklenmiştir.
  </para>
  <sect1 xml:id="bashref_bourne.shell.builtins-colon">
    <title>: (iki nokta üstüste)</title>
<indexterm scope="bashref_commands.index"><primary>: (iki nokta üstüste)</primary></indexterm>
    <para>
<synopsis><command>:</command> [<emphasis>argümanlar</emphasis>]
</synopsis>
    </para><para>
Yönlendirmelerin uygulanması ve <replaceable>argümanlar</replaceable>'ın yorumlanması dışında hiçbir şey yapılmaz. Dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-period">
    <title>. (nokta)</title>
<indexterm scope="bashref_commands.index"><primary>. (nokta)</primary></indexterm>
    <para>
<synopsis><command>.</command> <emphasis>dosyaismi</emphasis> [<emphasis>argümanlar</emphasis>]
</synopsis>
    </para><para>
Kullanılan kabuk kapsamında <replaceable>dosyaismi</replaceable>'nden komutlar okunur ve çalıştırılır. <replaceable>dosyaismi</replaceable> / içermiyorsa <replaceable>dosyaismi</replaceable>'nin yerini bulmak için <literal>PATH</literal> değişkeni kullanılır. Bash POSIX kipinde değilse ve <literal>$PATH</literal> içinde <replaceable>dosyaismi</replaceable> yoksa bulunulan dizine bakılır. Verilmiş <replaceable>argümanlar</replaceable> varsa, <replaceable>dosyaismi</replaceable> çalıştırılırken bunlar konumsal parametreler haline gelir. Aksi takdirde, konumsal parametreler değiştirilmez. Dönüş durumu son çalıştırılan komutun çıkış durumudur. Hiç komut çalıştırılmazsa sıfır durumu ile döner. <replaceable>dosyaismi</replaceable> bulunamazsa ya da okunamazsa, dönüş durumu sıfırdan faklı olur. Bu yerleşik <link linkend="bashref_bash.builtins-source"><command>source</command></link> yerleşiğine eşdeğerdir.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-break">
    <title><literal>break</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>break</primary></indexterm>
    <para>
<synopsis><command>break</command> [<emphasis>n</emphasis>]
</synopsis>
    </para><para>
<literal>for</literal>, <literal>while</literal>, <literal>until</literal> veya <literal>select</literal> döngülerinden çıkılmasını sağlar. <literal>n</literal> verilmişse, dışa doğru n. döngüden çıkılır. <literal>n >= 1</literal> olmalıdır, bu koşul sağlanıyorsa dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-cd">
    <title><literal>cd</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>cd</primary></indexterm>
    <para>
<synopsis><command>cd</command> [<command>-L</command>|<command>-P</command>] [<emphasis>dizin</emphasis>]
</synopsis>
    </para><para>
Bulunulan dizinden <replaceable>dizin</replaceable>'e geçilmesini sağlar. <replaceable>dizin</replaceable> verilmezse <literal>HOME</literal> kabuk değişkeni ile belirtilen dizine geçilir. <literal>CDPATH</literal> kabuk değişkeni mevcutsa bu, arama yolu olarak kullanılır. <replaceable>dizin</replaceable> bir / ile başlıyorsa <literal>CDPATH</literal> kullanılmaz.
    </para><para>
<option>-P</option> seçeneği verilmişse sembolik bağlar izlenmez. Sembolik bağların izlenmesi öntanımlıdır ve <option>-L</option> seçeneğine atanmıştır. <replaceable>dizin</replaceable> olarak <literal>-</literal> verilirse, <literal>$OLDPWD</literal> kullanılır.
    </para><para>
<command>CDPATH</command> değişkenindeki boş olmayan değer ya da ilk argüman olarak <command>-</command> kullanılmışsa ve dizin değişikliği gerçekleşmişse yeni çalışma dizininin mutlak dosyayolu standart çıktıya yazılır.
    </para><para>
Dizin değiştirme gerçekleşirse dönüş durumu sıfırdır, gerçekleşmezse sıfırdan farklıdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-continue">
    <title><literal>continue</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>continue</primary></indexterm>
    <para>
<synopsis><command>continue</command> [<emphasis>n</emphasis>]
</synopsis>
    </para><para>
<literal>for</literal>, <literal>while</literal>, <literal>until</literal> veya  <literal>select</literal> döngülerinin sonraki yinelemesinden devam edilir. <replaceable>n</replaceable> verilirse dışa doğru n. döngüden devam edilir. <literal>n >= 1</literal> olmalıdır, bu koşul sağlanıyorsa dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-eval">
    <title><literal>eval</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>eval</primary></indexterm>
    <para>
<synopsis><command>eval</command> [<emphasis>argümanlar</emphasis>]
</synopsis>
    </para><para>
<replaceable>argümanlar</replaceable> tek bir komut oluşturacak şekilde birleştirilip okunur ve çalıştırılır, çıkış durumu <command>eval</command>'ın çıkış durumu olarak döner. Hiç <replaceable>argüman</replaceable> verilmezse veya <replaceable>argümanlar</replaceable> boşsa sıfır durumu döner.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-exec">
    <title><literal>exec</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>exec</primary></indexterm>
    <para>
<synopsis><command>exec</command> [<command>-cl</command>] [<command>-a</command> <emphasis>isim</emphasis>] [<emphasis>komut</emphasis> [<emphasis>argümanlar</emphasis>]]
</synopsis>
    </para><para>
<replaceable>komut</replaceable> verilirse, yeni bir süreç oluşturulmadan kabuğa yerleştirilir. <option>-l</option> seçeneği verilmişse, <replaceable>komut</replaceable>'a aktarılan 0. argümanın başına kabuk bir tire işareti koyar. Bu <command>login</command> uygulamasının yaptığıdır. <option>-c</option> seçeneği <replaceable>komut</replaceable>'un boş bir ortamda çalıştırılmasına sebep olur. <option>-a</option> seçeneği verilirse kabuk, <replaceable>komut</replaceable>'a <replaceable>isim</replaceable>'i 0. argüman olarak aktarır. Bir <replaceable>komut</replaceable> verilmezse, kullanımdaki kabuk ortamını etkilemekte yönlendirmeler kullanılabilir. Yönlendirme hatası yoksa, dönüş durumu sıfırdır; aksi takdirde sıfırdan farklıdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-exit">
    <title><literal>exit</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>exit</primary></indexterm>
    <para>
<synopsis><command>exit</command> [<emphasis>n</emphasis>]
</synopsis>
    </para><para>
<replaceable>n</replaceable> durumu ile dönerek kabuk çıkar. <replaceable>n</replaceable> verilmezse son çalıştırılan komutun çıkış durumu döner. <literal>EXIT</literal> üzerindeki herhangi bir sinyal kapanı kabuk sonlanmadan önce çalıştırılır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-export">
    <title><literal>export</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>export</primary></indexterm>
    <para>
<synopsis><command>export</command> [<command>-fn</command>] [<command>-p</command>] [<emphasis>isim</emphasis>[=<emphasis>değer</emphasis>]]
</synopsis>
    </para><para>
Ortamdaki çocuk sürece aktarılacak her <replaceable>isim</replaceable>'i imler. <option>-f</option> seçeneği verilmişse <replaceable>isim</replaceable>'ler kabuk işlevleridir, aksi takdirde kabuk değişkenleridir. <option>-n</option> seçeneği verilirse aktarılacak <replaceable>isim</replaceable>'ler artık imlenmez. <replaceable>isim</replaceable> verilmemişse veya <option>-p</option> seçeneği verilmişse aktarılan <replaceable>isim</replaceable>'lerin listesi gösterilir. <option>-p</option> seçeneği çıktının girdi olarak tekrar kullanılabilir biçimde gösterilmesini sağlar. Bir değişken isminden sonra <literal>=</literal><varname>değer</varname> geliyorsa <varname>değer</varname> değişkenin değeri yapılır.
    </para><para>
İsimler geçersiz bir değişken ismi değilse veya <option>-f</option> seçeneği verilmişse ve isimlerin hepsi kabuk işlevi ise ya da geçersiz bir seçenek verilmemişse çıkış durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-getopts">
    <title><literal>getopts</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>getopts</primary></indexterm>
    <para>
<synopsis><command>getopts</command> <emphasis>seçenek-dizgesi</emphasis> [<emphasis>argümanlar</emphasis>]
</synopsis>
    </para><para>
<command>getopts</command> kabuk betikleri tarafından konumsal parametreleri çözümlemekte kullanılır. <replaceable>seçenek-dizgesi</replaceable> tanınan seçenek karakterlerini içerir; bir karakterden sonra bir iki nokta üstüste karakteri geliyorsa seçeneğin ondan bir boşlukla ayrılmış bir argümana sahip olduğu umulur. <literal>:</literal> ve <literal>?</literal> karakterleri seçenek karakterleri olarak kullanılamaz. Her çağrılışında <command>getopts</command> işlenecek sonraki argümanın indisini <literal>OPTIND</literal> değişkenine ve <replaceable>isim</replaceable> değişkenini de  mevcut değilse ilklendirerek sonraki seçeneği <replaceable>isim</replaceable> kabuk değişkenine yerleştirir. <literal>OPTIND</literal> kabuğun ya da bir kabuk betiğinin her çağrılışında 1 ile ilklendirilir. Bir seçenek bir argüman gerektirdiğinde <command>getopts</command> argümanı <literal>OPTARG</literal> değişkenine yerleştirir. Kabuk, <literal>OPTIND</literal> değişkenini otomatik olarak sıfırlamaz; eğer kullanılacak parametrelerin yeni bir kümesi varsa aynı kabuk çağrısı içinde <command>getopts</command>'a yapılan çoklu çağrılar arasında elle sıfırlanmalıdır.
    </para><para>
Seçeneklerin sonuna gelindiğinde, <command>getopts</command> sıfırdan büyük bir çıkış durumu ile çıkar. <literal>OPTIND</literal> ilk seçenek olmayan argümanın indisine ayarlanır ve <literal>?</literal> karakteri <replaceable>isim</replaceable>'e yerleştirilir.
    </para><para>
<command>getopts</command> normalde konumsal parametreleri çözümlese de <replaceable>argümanlar</replaceable> verilmişse bunları çözümler.
    </para><para>
<command>getopts</command> hataları iki yolla raporlayabilir. Eğer <replaceable>seçenek-dizgesi</replaceable>'nin ilk karakteri bir <literal>:</literal> ise <emphasis>sessiz</emphasis> hata raporlaması kullanılır. Geçersiz seçenekler veya eksik seçenekler saptandığında normal işlem tanı iletileri basılır. <literal>OPTERR</literal> değişkeninin değeri 0 ise <replaceable>seçenek-dizgesi</replaceable>'nin ilk karakteri bir <literal>:</literal> olmasa bile hata iletileri gösterilmez.
    </para><para>
Geçersiz bir seçenek görüldüğünde, <command>getopts</command> <literal>?</literal> karakterini <replaceable>isim</replaceable>'e yerleştirir ve hata raporlaması sessiz değilse bir hata iletisi basar ve <literal>OPTARG</literal> değişkenini kaldırır.
<command>getopts</command> sessiz hata raporlama kipinde ise bulunan seçenek karakterleri <literal>OPTARG</literal> değişkenine yerleştirilir ve hiçbir tanı iletisi basılmaz.
    </para><para>
Eğer bir gerekli argüman bulunamazsa ve <command>getopts</command> sessiz kipte değilse <replaceable>isim</replaceable>'e <literal>?</literal> karakteri yerleştirilir, <literal>OPTARG</literal> kaldırılır ve bir tanı iletisi basılır. <command>getopts</command> sessiz kipte ise <replaceable>isim</replaceable>'e <literal>:</literal> karakteri yerleştirilir ve <literal>OPTARG</literal>'a bulunan seçenek karakteri yerleştirilir.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-hash">
    <title><literal>hash</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>hash</primary></indexterm>
<indexterm scope="bashref_concept.index"><primary>komut tablosu</primary></indexterm>
    <para>
<synopsis><command>hash</command> [<command>-lr</command>] [<command>-p</command> <emphasis>dosyaismi</emphasis>] [<command>-dt</command>] [<emphasis>isim</emphasis>]
</synopsis>
    </para><para>
<replaceable>isim</replaceable> argümanları olarak belirtilen komutların tam dosya yollarını hatırlar, böylece müteakip çağrılarda aranmalarına gerek kalmaz. Komutlar <literal>$PATH</literal> içinde listelenmiş dizinler aranarak bulunur. <option>-p</option> seçeneği dosyayolu aramalarını engeller ve <replaceable>isim</replaceable>'in konumu olarak <replaceable>dosyaismi</replaceable> kullanılır. <option>-r</option> seçeneği hatırlanan tüm konumları kabuğun unutmasına sebep olur. <option>-d</option> seçeneği her <replaceable>isim</replaceable> için hatırlanan konumu kabuğun unutmasına sebep olur. <option>-t</option> seçeneği verildiğinde her isme karşılık gelen dosya konumları basılır. <option>-t</option> seçeneği ile çok sayıda <replaceable>isim</replaceable> verilirse, <replaceable>isim</replaceable> hatırlanan tam dosyayolundan önce basılır. <option>-l</option> seçeneği çıktının girdi olarak tekrar kullanılmasını sağlayacak biçimde basılmasını sağlar. Hiç argüman belirtilmezse ya da sadece <option>-l</option> seçeneği belirtilirse hatırlanan komutlar hakkında bilgi basılır. Bulunamayan bir <replaceable>isim</replaceable> yoksa veya geçersiz bir seçenek verilmemişse
dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-pwd">
    <title><literal>pwd</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>pwd</primary></indexterm>
    <para>
<synopsis><command>pwd</command> [<command>-LP</command>]
</synopsis>
    </para><para>
Bulunulan dizinin mutlak dosyayolunu basar. <literal>-P</literal> seçeneği verilirse basılan dosyayolu sembolik bağları içermez. <option>-L</option> seçeneği verilirse, basılan dosyayolu sembolik bağları içerebilir. Bulunulan dizinin ismi saptanırken bir hata oluşmamışsa ya da geçersiz bir seçenek verilmemişse dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-readonly">
    <title><literal>readonly</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>readonly</primary></indexterm>
    <para>
<synopsis><command>readonly</command> [<command>-apf</command>] [<emphasis>isim</emphasis>[=<emphasis>değer</emphasis>]] &hellip;
</synopsis>
    </para><para>
Her <replaceable>isim</replaceable>'i salt-okunur olarak imler. Bu <replaceable>isim</replaceable>'lerin değerleri müteakip çağrılarda değiştirilemez. <option>-f</option> seçeneği verilirse, her <replaceable>isim</replaceable> bir kabuk işlevidir. <literal>-a</literal> seçeneği verilirse, her <replaceable>isim</replaceable> bir dizi değişkenidir. Argüman olarak hiç <replaceable>isim</replaceable> verilmezse veya <option>-p</option> seçeneği verilirse salt-okunur isimlerin hepsi basılır. <option>-p</option> seçeneği gösterilen çıktının girdi olarak yeniden kullanılabilecek biçimde gösterilmesini sağlar. Bir değişken isminden sonra bir <literal>=</literal><varname>değer</varname> geliyorsa <varname>değer</varname> değişkenin değeri yapılır. Geçersiz bir seçenek verilmedikçe, <replaceable>isim</replaceable> argümanları bir geçersiz değişken ya da işlev ismi olmadıkça veya <option>-f</option> seçeneği ile verilen bir isim geçersiz bir kabuk işlevi değilse dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-return">
    <title><literal>return</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>return</primary></indexterm>
    <para>
<synopsis><command>return</command> [<emphasis>n</emphasis>]
</synopsis>
    </para><para>
Bir kabuk işlevinden <replaceable>n</replaceable> dönüş durumu ile çıkılmasına sebep olur. <replaceable>n</replaceable> verilmezse işlev içinde son çalıştırılan komutun çıkış durumu, dönüş durumu olur. Bu yerleşik ayrıca <command>.</command> (veya <command>source</command>) yerleşiği ile çalıştırılan bir betiğin dönüş durumu olarak ya <replaceable>n</replaceable> ya da son çalıştırılan komutun çıkış durumuyla sonlandırılmasında da kullanılabilir. <command>RETURN</command> sinyal kapanı ile ilişkilendirilmiş bir komut, icra betik veya işlevden sonra kaldığı yerden devam etmeden önce çalıştırılır. <command>return</command> bir işlevin dışında kullanılmışsa ve <command>return</command> kullanılan betiğin çalıştırılması <command>.</command> veya <command>source</command> yerleşikleri ile yapılmamışsa dönüş durumu sıfırdan farklıdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-shift">
    <title><literal>shift</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>shift</primary></indexterm>
    <para>
<synopsis><command>shift</command> [<emphasis>n</emphasis>]
</synopsis>
    </para><para>
Konumsal parametreleri <replaceable>n</replaceable> kadar sola kaydırır. İşlem sonunda <literal>$</literal><replaceable>n</replaceable><literal>>+1 ... $#</literal> konumsal   parametreleri <literal>$1 ... $#-</literal><replaceable>n</replaceable><literal>+1</literal> haline gelir. <literal>$#</literal>'dan <literal><emphasis>n</emphasis>+1</literal>'e kadar olan parametreler kaldırılır. <literal><replaceable>n</replaceable> >= 0</literal> ve <literal><replaceable>n</replaceable> =&lt; $#</literal> olmalıdır. <replaceable>n</replaceable> sıfır ya da  <literal>$#</literal> den büyükse konumsal parametreler değişmez. <replaceable>n</replaceable> verilmezse, öntanımlı olarak 1 kabul edilir. <replaceable>n</replaceable><literal> > $#</literal> ve <replaceable>n</replaceable><literal> &lt; 0</literal> değilse dönüş durumu sıfırdır, aksi takdirde sıfırdan farklıdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-test">
    <title><literal>test</literal> Yerleşiği -- [</title>
<indexterm scope="bashref_commands.index"><primary>[</primary></indexterm>
<indexterm scope="bashref_commands.index"><primary>test</primary></indexterm>
    <para>
<synopsis><command>test</command> [<emphasis>ifade</emphasis>]

<command>[</command> [<emphasis>ifade</emphasis>] ]
</synopsis>
    </para><para>
Koşullu <replaceable>ifade</replaceable> değerlendirilir. Her işleç ve terim ayrı bir argüman olmalıdır. İfadeler <xref linkend="bashref_conditionals"/> bölümünde açıklanan önceliklerin birleşiminden oluşur. <command>test</command> seçeneklerin sonunu imleyen <command>-</command> argümanını kabul etmeyip yoksaydığı gibi bir seçenek de kabul etmez.
    </para><para>
<command>[</command> kullanımında komutun son argümanı bir <literal>]</literal> olmalıdır.
    </para><para>
İfadeler aşağıdaki işleçler kullanılarak birleştirilebilir. İşleçlerin öncelikleri verildikleri sıradadır.
    </para><glosslist><glossentry>
<glossterm><literal>! </literal><replaceable>ifade</replaceable></glossterm>
<glossdef><para>
<replaceable>ifade</replaceable> yanlışsa sonuç doğrudur.
</para></glossdef></glossentry><glossentry>
<glossterm><literal>( </literal><replaceable>ifade </replaceable><literal>)</literal></glossterm>
<glossdef><para>
Sonuç <replaceable>ifade</replaceable>'nin değeridir. Bu kullanım işlecin normal önceliğini arttırmak içindir.
</para></glossdef></glossentry><glossentry>
<glossterm><replaceable>ifade1 </replaceable><literal>-a </literal><replaceable>ifade2</replaceable></glossterm>
<glossdef><para>
Hem <replaceable>ifade1</replaceable> hem de <replaceable>ifade2</replaceable> doğru ise sonuç doğrudur.
</para></glossdef></glossentry><glossentry>
<glossterm><replaceable>ifade1 </replaceable><literal>-o </literal><replaceable>ifade2</replaceable></glossterm>
<glossdef><para>
<replaceable>ifade1</replaceable> veya <replaceable>ifade2</replaceable> doğru ise sonuç doğrudur.
</para></glossdef></glossentry></glosslist><para>
<command>test</command> ve <command>[</command> yerleşikleri koşullu ifadeleri argümanlarının sayısına bağlı bazı kurallara göre değerlendirir.
    </para><glosslist>
<glossentry><glossterm>0 argüman</glossterm>
<glossdef><para>İfade yanlıştır.</para></glossdef>
</glossentry><glossentry><glossterm>1 argüman</glossterm>
<glossdef><para>
Sadece ve sadece argüman boş değilse ifade doğrudur.
</para></glossdef></glossentry><glossentry>
<glossterm>2 argüman</glossterm><glossdef><para>
İlk argüman <literal>!</literal> ise ve ikinci argüman sadece ve sadece boş ise ifade doğrudur. İlk argüman tek terimli koşul işleçlerinden biri ise (<xref linkend="bashref_conditionals"/> bölümüne bakınız) ve tek terimlinin sonucu doğru ise ifadenin sonucu doğrudur. İlk argüman geçerli bir tek terimli işleç değilse, ifade yanlıştır.
</para></glossdef></glossentry><glossentry>
<glossterm>3 argüman</glossterm><glossdef><para>
İkinci argüman iki terimli koşul işleçlerinden biri ise (<xref linkend="bashref_conditionals"/> bölüne bakınız), ifadenin sonucu ilk ve üçüncü argümanların terimleri olduğu iki terimlinin sonucudur. İlk argüman <literal>!</literal> ise sonuç, ikinci ve üçüncü argümanın kullanıldığı iki argümanlı sınamanın zıddıdır. İlk argüman tek başına <literal>(</literal> ve üçüncü argüman tek başına <literal>)</literal> ise sonuç, ikinci argümanın tek terimli sınamasının sonucudur. Aksi takdirde, ifade yanlıştır. <option>-a</option> ve <option>-o</option> işleçlerinin bu durumda iki terimli işleçleri olduğu varsayılır.
</para></glossdef></glossentry><glossentry>
<glossterm>4 argüman</glossterm><glossdef><para>
İlk argüman <literal>!</literal> ise sonuç, kalan argümanların oluşturduğu üç terimli ifadenin sonucudur. Aksi takdirde, ifade yukarıdaki kurallar kullanılarak önceliklere göre çözümlenir ve değerlendirilir.
</para></glossdef></glossentry><glossentry>
<glossterm>5 argüman ve fazlası</glossterm><glossdef><para>
İfade yukarıdaki kurallar kullanılarak önceliklere göre çözümlenir ve değerlendirilir.
</para></glossdef>
</glossentry></glosslist>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-times">
    <title><literal>times</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>times</primary></indexterm>
    <para>
<synopsis><command>times</command>
</synopsis>
    </para><para>
Kabuk ve altkabukları tarafında kullanılan kullanıcı ve sistem sürelerini
gösterir. Dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-trap">
    <title><literal>trap</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>trap</primary></indexterm>
<indexterm scope="bashref_concept.index"><primary>sinyal</primary> <secondary>kapanı</secondary></indexterm>
    <para>
<synopsis><command>trap</command> [<command>-lp</command>] [<emphasis>argüman</emphasis>] [<emphasis>sinyal</emphasis> &hellip;]
</synopsis>
    </para><para>
<replaceable>argüman</replaceable> içindeki komutlar, kabuk <replaceable>sinyal</replaceable> sinyalini aldığında okunur ve çalıştırılır<footnote><para> Bir betik çalışırken klavyeden <literal>^C</literal> tuşladığınızda durmamasını isterseniz aşağıdaki gibi bir programcığı betiğin başına yerleştirebilirsiniz (fare kapanı kurmak gibi sinyal kapanı kuruyoruz):
    </para><para xml:id="bashref_sinyal-kapani">
<screen># Ctrl + C tuşlandığında gösterilecek ileti
<command>trap 'echo "Control-C iptal edildi."' SIGINT</command>
</screen>
</para></footnote>. <replaceable>argüman</replaceable> bir boş dizge ise her <replaceable>sinyal</replaceable> sinyali kabuk ve onu çağıran komutlar tarafından yoksayılır<footnote><para>
<screen># Hiçbir ileti göstermeden de ^C iptal edilebilir
<command>trap '' SIGINT</command>
</screen>
</para></footnote>. <replaceable>argüman</replaceable> verilmezse (ve tek bir <replaceable>sinyal</replaceable> varsa) ya da <literal>-</literal> verilirse, belirtilen tüm sinyallere kabuk başlatıldığındaki değerleri yerleştirilir. <footnote><para><literal>^C</literal> kesmesini tekrar etkin kılmak isterseniz:
</para><para>
<screen>
<command>trap SIGINT</command>
</screen>
</para><para>yeterlidir.
</para></footnote>. Sadece <option>-p</option> seçeneği verilmişse <command>trap</command>, her sinyal numarası ile ilişkili komutların listesini kabuk girdisi olarak kullanılabilecek biçimde gösterir. <option>-l</option> seçeneği kabuğun sinyal isimlerini numaraları ile birlikte listelemesini sağlar. Her <replaceable>sinyal</replaceable> ya bir sinyal ismi ya da bir sinyal numarası olarak verilmelidir. Sinyal isimleri harf büyüklüğüne duyarlıdır ve <literal>SIG</literal> öneki isteğe bağlıdır. Eğer <replaceable>sinyal</replaceable> <literal>0</literal> ya da <literal>EXIT</literal> ise <replaceable>argüman</replaceable> kabuk çıkarken çalıştırılır. Eğer <replaceable>sinyal</replaceable> olarak <literal>DEBUG</literal> verilmişse, <replaceable>argüman</replaceable> komutu her basit komuttan önce, <command>for</command> komutundan, <command>case</command> komutundan, <command>select</command> komutundan, her aritmetik <command>for</command> komutundan ve bir kabuk işlevinde çalıştırılacak ilk komuttan önce çalıştırılır. <literal>DEBUG</literal> kapanının etkisi ile ilgili daha ayrıntılı açıklama için <command>shopt</command> yerleşiğinin <link linkend="bashref_shopt-extglob"><option>extglob</option> seçeneğinin açıklamasına</link> bakınız. Eğer bir <replaceable>sinyal</replaceable> <literal>ERR</literal> ise <replaceable>argüman</replaceable> komutu, aşağıdaki koşullara konu olan sıfırdan farklı çıkış durumuna sahip her komut için çalıştırılır. Başarısız komut, hemen ardından <command>until</command> veya <command>while</command> gelen bir komut listesinin parçasıysa ya da <command>if</command> deyiminde sınamanın bir parçasıysa veya bir <command>&amp;&amp;</command> veya <command>||</command> listesinin bir parçasıysa veya komutun dönüş durumu <literal>!</literal> ile zıddına ayarlanmışsa, <literal>ERR</literal> sinyal kapanı çalıştırılmaz. Bunlar <command>errexit</command> seçeneği tarafından uyulan koşullarla aynıdır. <replaceable>sinyal</replaceable> <command>RETURN</command> ise <replaceable>argüman</replaceable> ile belirtilen komut çalıştırılır.
    </para><para>
Kabuk girdilerinde yoksayılan sinyaller yakalanamaz ve sıfırlanamaz. Yoksayılmayıp yakalanan sinyallere alt süreç içinde oluşturuldukları zamanki özgün değerleri atanır.
    </para><para>
Bir <replaceable>sinyal</replaceable> geçersiz bir sinyal belirtmedikçe yerleşiğin dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-umask">
    <title><literal>umask</literal> Yerleşiği</title>
    <indexterm scope="bashref_commands.index"><primary>umask</primary></indexterm>
    <para>
<synopsis><command>umask</command> [<command>-p</command>] [<command>-S</command>] [<emphasis>kip</emphasis>]
</synopsis>
    </para><para>
Kabuk sürecinin dosya oluşturma maskesini <replaceable>kip</replaceable> olarak ayarlar. <replaceable>kip</replaceable> bir rakam ile başlıyorsa sekizlik bir sayı olarak yorumlanır; rakamla başlamıyorsa, <command>chmod</command> komutundaki gibi bir sembolik kip maskesi olarak yorumlanır. Eğer <replaceable>kip</replaceable> verilmezse, maskenin mevcut değeri gösterilir. <option>-S</option> seçeneği bir <replaceable>kip</replaceable> olmaksızın verilirse, maske sembolik kipte gösterilir. <option>-p</option> seçeneği bir <replaceable>kip</replaceable> olmaksızın verilirse, maske, kabuğa girdi olarak verilebilecek biçemde gösterilir. <replaceable>kip</replaceable> değiştirilebilmişse veya <replaceable>kip</replaceable> argümanı verilmemişse dönüş durumu sıfırdır, aksi takdirde sıfırdan farklıdır.
    </para><para>
<note><para><replaceable>kip</replaceable> bir sekizlik sayı olarak yorumlandığında, maske içindeki her numara 7 den çıkarılır. Yani, <literal>022</literal> olarak verilen maske, <literal>755</literal> izinlerine karşılıktır.</para></note>
    </para>
  </sect1>
  <sect1 xml:id="bashref_bourne.shell.builtins-unset">
    <title><literal>unset</literal> Yerleşiği</title>
    <indexterm scope="bashref_commands.index"><primary>unset</primary></indexterm>
    <para>
<literallayout indent="0">
    <command>unset</command> [-fv] [<emphasis>isim</emphasis>]
</literallayout>
    </para><para>
<replaceable>isim</replaceable>'i belirtilen her işlev ya da değişken kaldırılır. Hiçbir seçenek verilmemişse ya da <option>-v</option> seçeneği verilmişse, her <replaceable>isim</replaceable> bir kabuk değişkenidir. <option>-f</option> seçeneği verilmişse, her <replaceable>isim</replaceable> bir kabuk işlevidir ve işlev tanımı kaldırılır. Salt-okunur değişkenler ve işlevler kaldırılamaz. Belirtilen <replaceable>isim</replaceable> salt-okunur olmadıkça dönüş durumu sıfırdır.
    </para>
  </sect1>
</chapter>

<chapter xml:id="bashref_bash.builtins" label="2">
  <title>Bash Yerleşik Komutları</title>
  <titleabbrev>Bash'e özel yerleşik komutlar.</titleabbrev>
<indexterm scope="bashref_concept.index">
<primary>yerleşik komutlar</primary><secondary>Bash</secondary></indexterm>
  <para>
Bu kısımda eşsiz veya Bash'e sonradan eklenmiş yerleşik komutlardan bahsedilmiştir. Buradaki komutlardan bazıları POSIX standardında belirtilmiştir.
  </para>
  <sect1 xml:id="bashref_bash.builtins-alias">
    <title><literal>alias</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>alias</primary></indexterm>
    <para>
<synopsis><command>alias</command> [<command>-p</command>] [<emphasis>isim</emphasis>[=<emphasis>değer</emphasis>] ...]
</synopsis>
    </para><para>
Argümansız veya <option>-p</option> seçeneği ile <command>alias</command>, kabukta girdi olarak yeniden kullanılabilecek biçemde takma adların listesini gösterir. Argümanlar verilmişse, her <replaceable>isim</replaceable> takma ad olarak alınır ve <replaceable>değer</replaceable> bu takma ada atanır. Eğer <replaceable>değer</replaceable> verilmemişse, takma adın <replaceable>isim</replaceable> ve <replaceable>değer</replaceable>'i basılır. Takma adlar <xref linkend="bashref_aliases"/> bölümünde anlatılmıştır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-bind">
    <title><literal>bind</literal> Yerleşiği</title>
    <indexterm scope="bashref_commands.index"><primary>bind</primary></indexterm>
    <para>
<synopsis><command>bind</command> [<command>-m</command> <emphasis>tuşeşlem</emphasis>] [<command>-lpsvPSV</command>]
<command>bind</command> [<command>-m</command> <emphasis>tuşeşlem</emphasis>] [<command>-q</command> <emphasis>işlev</emphasis>] [<command>-u</command> <emphasis>işlev</emphasis>] [<command>-r</command> <emphasis>tuş-dizisi</emphasis>]
<command>bind</command> [<command>-m</command> <emphasis>tuşeşlem</emphasis>] <command>-f</command> <emphasis>dosyaismi</emphasis>
<command>bind</command> [<command>-m</command> <emphasis>tuşeşlem</emphasis>] <command>-x</command> <emphasis>tuş-dizisi</emphasis>:<emphasis>kabuk-komutu</emphasis>
<command>bind</command> [<command>-m</command> <emphasis>tuşeşlem</emphasis>] <emphasis>tuş-dizisi</emphasis>:<emphasis>işlev-ismi</emphasis>
<command>bind</command> <emphasis>readline-komutu</emphasis>]
</synopsis>
    </para><para>
Kullanımdaki Readline (<xref linkend="bashref_command.line.editing"/> bölümüne bakınız) tuş ve işlev kısayollarını gösterir, bir Readline işlevi ya da makrosunu bir tuş dizisine bağlar veya bir Readline değişkenini atar. Seçenek olmayan her argüman bir <link linkend="bashref_rl.initfiles">Readline ilklendirme dosyası</link>ndaki gibi bir komuttur, fakat her kısayol veya komut ayrı bir argüman olarak verilmilidir; örn. <literal>"\C-x\C-r":re-read-init-file</literal>. Seçenekler verildiğinde aşağıdaki anlamlara gelir:
    </para><glosslist><glossentry>
<glossterm><command>-m </command><replaceable>tuşeşlemi</replaceable></glossterm>
<glossdef><para>
Müteakip bağlantıların etkileneceği tuşeşlemi olarak <replaceable>tuşeşlemi</replaceable> kullanılır. Burada geçerli <replaceable>tuşeşlemi</replaceable> isimleri,<literal>emacs</literal>, <literal>emacs-standard</literal>, <literal>emacs-meta</literal>, <literal>emacs-ctlx</literal>, <literal>vi</literal>, <literal>vi-move</literal>, <literal>vi-command</literal> ve <literal>vi-insert</literal>'dir. <literal>vi</literal> ile <literal>vi-command</literal> ve <literal>emacs</literal> ile <literal>emacs-standard</literal> eşdeğerdir.
</para></glossdef></glossentry><glossentry>
<glossterm><command>-l</command></glossterm>
<glossdef><para>
Tüm Readline işlevlerini listeler.
</para></glossdef></glossentry><glossentry>
<glossterm><command>-p</command></glossterm>
<glossdef><para>
Readline işlev isimlerini ve kısayollarını, girdi olarak ya da bir Readline ilklendirme dosyasında kullanılabilir biçemde gösterir.
</para></glossdef></glossentry><glossentry>
<glossterm><command>-P</command></glossterm>
<glossdef><para>
Kullanımdaki Readline işlev isimlerini ve kısayollarını listeler.
</para></glossdef></glossentry><glossentry>
<glossterm><command>-v</command></glossterm>
<glossdef><para>
Readline değişken isimlerini ve değerlerini, girdi olarak ya da bir Readline ilklendirme dosyasında kullanılabilir biçemde gösterir.
</para></glossdef></glossentry><glossentry>
<glossterm><command>-V</command></glossterm>
<glossdef><para>
Kullanımdaki Readline değişken isimlerini ve değerlerini listeler.
</para></glossdef></glossentry><glossentry>
<glossterm><command>-s</command></glossterm>
<glossdef><para>
Makrolara ve dizgelere bağlı Readline tuş dizilerini, girdi olarak ya da bir Readline ilklendirme dosyasında kullanılabilir biçemde gösterir.
</para></glossdef></glossentry><glossentry>
<glossterm><command>-S</command></glossterm>
<glossdef><para>
Makrolara ve dizgelere bağlı Readline tuş dizilerini gösterir.
</para></glossdef></glossentry><glossentry>
<glossterm><command>-f </command><replaceable>dosyaismi</replaceable></glossterm>
<glossdef><para>
Tuş kısayollarını <replaceable>dosyaismi</replaceable>'nden okur.
</para></glossdef></glossentry><glossentry>
<glossterm><command>-q </command><replaceable>işlev</replaceable></glossterm>
<glossdef><para>
İsmi belirtilen <replaceable>işlev</replaceable>'i çağıran tuşlar hakkında sorgu.
</para></glossdef></glossentry><glossentry>
<glossterm><command>-u </command><replaceable>işlev</replaceable></glossterm>
<glossdef><para>
İsmi belirtilen <replaceable>işlev</replaceable>'i çağıran tüm tuş kısayollarını kaldırır.
</para></glossdef></glossentry><glossentry>
<glossterm><command>-r </command><replaceable>tuş-dizisi</replaceable></glossterm>
<glossdef><para>
<replaceable>tuş-dizisi</replaceable> için mevcut bağlantıları kaldırır.
</para></glossdef></glossentry><glossentry>
<glossterm><command>-x </command><replaceable>tuş-dizisi</replaceable><command>:</command><replaceable>kabuk-komutu</replaceable></glossterm>
<glossdef><para>
<replaceable>tuş-dizisi</replaceable>'nin her girilişinde <replaceable>kabuk-komutu</replaceable>'nun çalıştırılmasını sağlar.
</para></glossdef></glossentry></glosslist><para>
Geçersiz bir seçenek verilmedikçe veya bir hata oluşmadıkça dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-builtin">
    <title><literal>builtin</literal>  Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>builtin</primary></indexterm>
    <para>
<synopsis><command>builtin</command> [<emphasis>kabuk-yerleşiği</emphasis> [<emphasis>argümanlar</emphasis>]]
</synopsis>
    </para><para>
Bir <replaceable>kabuk-yerleşiği</replaceable>'ne <replaceable>argümanlar</replaceable>'ı aktararak çalıştırır. Bu bir kabuk işlevinin bir kabul yerleşiği ile aynı isimde atanması durumunda faydalıdır. <replaceable>kabuk-yerleşiği</replaceable> geçerli bir kabuk yerleşiği değilse dönüş durumu sıfırdan farklıdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-caller">
    <title><literal>caller</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>caller</primary></indexterm>
    <para>
<synopsis><command>caller</command> [<emphasis>ifade</emphasis>]
</synopsis>
    </para><para>
Etkin altyordam çağrısının (<command>.</command> veya <command>source</command> ile çalıştırılan bir betik) bağlamı ile döner.
      </para><para>
<varname>ifade</varname> olmaksızın, <command>caller</command> o anki altyordam çağrısının kaynak dosya ismini ve satır numarasını gösterir. Eğer <varname>ifade</varname> olarak negatiften farklı bir tamsayı belirtilmişse <command>caller</command> o anki icra çağrısı yığıtındaki o konuma karşılık gelen kaynak dosyası, satır numarası ve altyordam ismini gösterir. Bu ek bilgi örneğin bir yığıt izini basmakta kullanılabilir. Anlık çerçeve 0. çerçevedir.
      </para><para>
Kabuk bir altyordam çağrısını çalıştırmıyor olmadıkça dönüş durumu 0'dır, aksi takdirde <varname>ifade</varname>  çağrı yığıtında geçerli bir konuma karşılık değildir.
      </para>
    </sect1>
  <sect1 xml:id="bashref_bash.builtins-command">
    <title><literal>command</literal> Yerleşiği</title>
    <indexterm scope="bashref_commands.index"><primary>command</primary></indexterm>
    <para>
<synopsis><command>command</command> [<command>-pVv</command>] <emphasis>komut</emphasis> [<emphasis>argümanlar</emphasis> ...]
</synopsis>
    </para><para>
<replaceable>komut</replaceable> komutunu <replaceable>komut</replaceable> isimli kabuk işlevini yoksayarak <replaceable>argümanlar</replaceable> ile çalıştırır. Sadece kabuk yerleşiklerini ve <literal>PATH</literal> araması ile bulunan komutları çalıştırır. <function>ls</function> isimli bir kabuk işlevi varsa ve işlevin içinde <command>command ls</command> varsa, işlev çağrısında işlev ardışık olarak çağrılmak yerine <command>ls</command> komutu çalıştırılacaktır. <option>-p</option> seçeneği, tüm standart uygulamaların bulunmasını garanti eden <literal>PATH</literal> için bir öntanımlı değerle kullanılacağı anlamına gelir. Bu durumda <replaceable>komut</replaceable> bulunamazsa veya bir hata oluşursa 127 durumu ile aksi takdirde <replaceable>komut</replaceable>'un çıkış durumu ile döner.
    </para><para>
<option>-V</option> ya da <option>-v</option> seçeneği verilmişse, bir <replaceable>komut</replaceable> açıklaması basılır. <option>-v</option> seçeneği komutu çağırmada kullanılan dosya ismini ya da komutu belirten tek bir sözcük gösterilmesini sağlar. <option>-V</option> seçeneği ise daha ayrıntılı açıklama gösterilmesini sağlar. Bu durumda dönüş durumu <replaceable>komut</replaceable> bulunursa sıfır, aksi takdirde sıfırdan farklı olur.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-declare">
    <title><literal>declare</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>declare</primary></indexterm>
    <para>
<synopsis><command>declare</command> [<command>-afFirtx</command>] [<command>-p</command>] [<emphasis>isim</emphasis>[=<emphasis>değer</emphasis>] &hellip;]
</synopsis>
    </para><para>
Değişkenlerin özellikleri ile bildirilmesini sağlar. Hiçbir <replaceable>isim</replaceable> belirtilmezse değişkenleri ve değerlerini listeler.
    </para><para>
<option>-p</option> seçeneği ile her <replaceable>isim</replaceable> için değerini ve özellikleri gösterecektir. <option>-p</option> seçeneği ile birlikte verilen diğer seçenekler yoksayılır. <option>-F</option> seçeneği işlev tanımlarının basılmasını engeller; sadece işlev isimleri ve özellikleri gösterilir. <command>extdebug</command> kabuk seçeneği <command>shopt</command> kullanarak etkinleştirilmişse ek olarak işlevin tanımlandığı kaynak dosya ismi ve satır numarası gösterilir. <option>-F</option> seçeneği <option>-f</option> seçeneğini uygular. Aşağıdaki seçenekler değişkenlere belirtilen özelliklerle çıktılanmasını veya değişken özelliklerinin verilmesini sınırlamakta kullanılabilir:
    </para><variablelist>
<varlistentry><term><literal>-a</literal></term>
<listitem><para>
Her <replaceable>isim</replaceable> bir <link linkend="bashref_arrays">dizi</link> değişkenidir.
</para></listitem></varlistentry>
<varlistentry><term><literal>-f</literal></term>
<listitem><para>
Sadece işlev isimleri kullanılır.
</para></listitem></varlistentry>
<varlistentry><term><literal>-i</literal></term>
<listitem><para>
Değişken bir tamsayı olarak ele alınır ve değişkene bir değer atandığında <link linkend="bashref_shell.arithmetic">aritmetik değerlendirme</link> uygulanır.
</para></listitem></varlistentry>
<varlistentry><term><literal>-r</literal></term>
<listitem><para>
İsimleri salt-okunur yapar. Bu isimlere sonradan değer atanamaz, değiştirilemez ve kaldırılamaz.
</para></listitem></varlistentry>
<varlistentry><term><literal>-t</literal></term>
<listitem><para>
Her <varname>isim</varname>'e <command>trace</command> özniteliği verir. İzlenen işlevler <command>DEBUG</command> ve <command>RETURN</command> tuzaklarını çağıran kabuktan miras alırlar. <command>trace</command>  özniteliğinin değişkenler için özel bir anlamı yoktur.
</para></listitem></varlistentry>
<varlistentry><term><literal>-x</literal></term>
<listitem><para>
Her ismi müteakip komutlara ortam üzerinden aktarılması için imler.
</para></listitem></varlistentry>
</variablelist><para>
<literal>-</literal> yerine <literal>+</literal> kullanılarak özellik kapatılabilir. Bir işlevde kullanıldığında <command>declare</command> her <replaceable>isim</replaceable>'i <command>local</command> komutu kullanılmış gibi yerel yapar. Eğer değişken isminden hemen sonra bir <literal>=</literal><varname>değer</varname> varsa <varname>değer</varname> değişkene atanır.
    </para><para>
Şu hallerin gerçekleşmemesi şartıyla çıkış durumu sıfırdır: bir geçersiz seçenek saptanırsa, <literal>-f foo=bar</literal> kullanarak bir işleve atama yapmaya çalışıldığında, bir salt-okunur değişkene bir değer atanmaya çalışıldığında, bir dizi değişkenine birleşik atama sözdizimi (<xref linkend="bashref_arrays"/> bölümüne bakınız) kullanmaksızın bir değer atanmaya çalışıldığında, <replaceable>isim</replaceable>'lerden biri geçersiz bir kabuk değişkeni ismi ise, bir salt-okunur değişkenin salt-okunurluğu kaldırılmak istendiğinde, bir dizi değişkeninden dizi olmasını ortadan kaldıracak bir atama yapılmaya çalışıldığında veya mevcut olmayan bir işlevin <option>-f</option> seçeneği ile gösterilmesi istendiğinde.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-echo">
    <title><literal>echo</literal> Yerleşiği</title>
    <indexterm scope="bashref_commands.index"><primary>echo</primary></indexterm>
    <para>
<synopsis><command>echo</command> [<command>-neE</command>] [<emphasis>argümanlar</emphasis> ...]
</synopsis>
    </para><para>
Boşluklarla ayrılmış, bir satırsonu ile sonlandırılmış <replaceable>argümanlar</replaceable> çıktılanır. Dönüş durumu daima sıfırdır. <option>-n</option> seçeneği ile satırsonu karakteri baskılanır. <option>-e</option> seçeneği ile aşağıdaki tersbölü öncelemeli karakterlerin yorumlanması etkinleştirilir. <option>-E</option> seçeneği bu öncelenmiş karakterlerinin yorumlanmasının öntanımlı olarak etkin olduğu sistemlerde bile yorumlamayı kapatır. <option>xpg_echo</option> kabuk seçeneği öntanımlı olarak bu öncelenmiş karakterlerin yorumlanıp yorumlanmayacağının dinamik olarak saptanması için kullanılabilir. <command>echo</command> <command>-</command>'yi seçeneklerin sonu anlamında yorumlamaz.
    </para><para>
<command>echo</command> aşağıdaki öncelenmiş dizilimleri yorumlar:
    </para><variablelist>
<varlistentry><term><literal>\a</literal></term>
<listitem><para>sesli uyarı (bell)
</para></listitem></varlistentry>
<varlistentry><term><literal>\b</literal></term>
<listitem><para>gerisilme
</para></listitem></varlistentry>
<varlistentry><term><literal>\c</literal></term>
<listitem><para>kendisinden sonra gelen satırsonu karakterini kaldırır.
</para></listitem></varlistentry>
<varlistentry><term><literal>\e</literal></term>
<listitem><para>escape
</para></listitem></varlistentry>
<varlistentry><term><literal>\f</literal></term>
<listitem><para>sayfa ilerletme
</para></listitem></varlistentry>
<varlistentry><term><literal>\n</literal></term>
<listitem><para>satırsonu
</para></listitem></varlistentry>
<varlistentry><term><literal>\r</literal></term>
<listitem><para>satırbaşı
</para></listitem></varlistentry>
<varlistentry><term><literal>\t</literal></term>
<listitem><para>yatay sekme
</para></listitem></varlistentry>
<varlistentry><term><literal>\v</literal></term>
<listitem><para>düşey sekme
</para></listitem></varlistentry>
<varlistentry><term><literal>\\</literal></term>
<listitem><para>tersbölü
</para></listitem></varlistentry>
<varlistentry><term><literal>\0</literal><varname>nnn</varname></term>
<listitem><para>
sekizlik değeri <varname>nnn</varname> olan sekiz bitlik karakter (0, 1, 2 ya da 3 haneli olabilir)
</para></listitem></varlistentry>
<varlistentry><term><literal>\x</literal><varname>hh</varname></term>
<listitem><para>
onaltılık değeri <varname>hh</varname> olan sekiz bitlik karakter (1 ya da 2 haneli olabilir)
</para></listitem></varlistentry>
</variablelist>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-enable">
    <title><literal>enable</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>enable</primary></indexterm>
    <para>
<synopsis><command>enable</command> [<command>-n</command>] [<command>-p</command>] [<command>-f</command> <emphasis>dosyaismi</emphasis>] [<command>-ads</command>] [<emphasis>isim</emphasis> ...]
</synopsis>
    </para><para>
Yerleşik kabuk komutlarını etkinleştirir ve kaldırır. Bir yerleşiğin iptal edilmesi durumunda, kabuk, yerleşik komutlara diğer komutlardan önce bakmasına rağmen bir tam dosyayolu belirtilmeksizin aynı isimdeki bir uygulamanın kabuk komutu olarak kullanılması sağlanır. Bu, <option>-n</option> seçeneği ile <replaceable>isim</replaceable>'leri belirtilen yerleşikler için uygulanır, seçenek verilmezse yerleşikler etkinleştirilir. Örneğin <command>test</command> yerleşiği yerine <command>test</command> uygulamasının yerleşik komut olarak kullanılmasını sağlamak için <command>enable -n test</command> yazmalısınız.
    </para><para>
<option>-p</option> seçeneği verilirse ya da <replaceable>isim</replaceable> argümanları verilmezse kabuk yerleşikleri listelenir. Başka hiçbir argüman verilmezse tüm etkin kabuk yerleşikleri listelenir. <option>-a</option> seçeneği ile her yerleşik etkin olup olmadığı belirtilerek listelenir.
    </para><para>
Dinamik yüklemenin desteklendiği sistemlerde, <option>-f</option> seçeneği ile yeni bir <replaceable>isim</replaceable> yerleşik komutunun <replaceable>dosyaismi</replaceable> ile belirtilen paylaşımlı nesneden yüklenmesi sağlanır. <option>-d</option> seçeneği <option>-f</option> ile yüklenen yerleşiği silmek için kullanılır.
    </para><para>
Hiç seçenek verilmezse kabuk yerleşiklerinin bir listesi gösterilir. <option>-s</option> seçeneği POSIX'e özel yerleşiklerin etkinleştirilmesini sınırlar. <option>-s</option> seçeneği <option>-f</option> seçeneği ile birlikte kullanılırsa yeni yerleşik bir <link linkend="bashref_special.builtins">özel yerleşik</link> haline gelir.
    </para><para>
Bir <replaceable>isim</replaceable> geçersiz bir kabuk yerleşiği olmadıkça ya da bir paylaşımlı nesneden yeni bir yerleşiği yüklerken hata olmadıkça dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-help">
    <title><literal>help</literal> Yerleşiği</title>
    <indexterm scope="bashref_commands.index"><primary>help</primary></indexterm>
    <para>
<synopsis><command>help</command> [<command>-s</command>] [<emphasis>kalıp</emphasis>]
</synopsis>
    </para><para>
Yerleşik komutlar hakkında yardım bilgisi gösterir. <replaceable>kalıp</replaceable> belirtilmişse, <command>help</command> <replaceable>kalıp</replaceable> ile eşleşen tüm komutlar için yardım bilgisi gösterir, aksi takdirde yerleşikler listelenir. <option>-s</option> seçeneği ile yardım bilgisi yerine komutun kullanımını gösteren sözdizimini basar. <replaceable>kalıp</replaceable> ile eşleşme olmaması dışında dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-let">
    <title><literal>let</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>let</primary></indexterm>
    <para>
<synopsis><command>let</command> <emphasis>ifade</emphasis> [<emphasis>ifade</emphasis>]
</synopsis>
    </para><para>
Kabuk değişkenleri üzerinde aritmetik işlemler uygulanmasını sağlar. Her <replaceable>ifade</replaceable> <xref linkend="bashref_shell.arithmetic"/> bölümünde açıklanan kurallara göre değerlendirilir. Son <replaceable>ifade</replaceable>'nin sonucu 0 ise dönüş durumu 1 dir, aksi takdirde 0 dır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-local">
    <title><literal>local</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>local</primary></indexterm>
    <para>
<synopsis><command>local</command> [<emphasis>seçenek</emphasis>] <emphasis>isim</emphasis>[=<emphasis>değer</emphasis>] &hellip;
</synopsis>
    </para><para>
Her argüman için <replaceable>isim</replaceable> isimli bir yerel değişken oluşturulur ve <replaceable>değer</replaceable> atanır. <replaceable>seçenek</replaceable>, <command>declare</command> tarafından kabul edilen seçenekler olabilir. <command>local</command> sadece bir işlev içinde kullanılabilir; <replaceable>isim</replaceable> değişkeninin sadece işlev ve çocuklarının etki alanında görünür olmasını sağlar. <command>local</command> bir işlevin dışında kullanılmış ya da <replaceable>isim</replaceable> bir salt-okunur değişken olmadıkça dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-logout">
    <title><literal>logout</literal> Yerleşiği</title>
    <indexterm scope="bashref_commands.index"><primary>logout</primary></indexterm>
    <para>
<synopsis><command>logout</command> [<emphasis>n</emphasis>]
</synopsis>
    </para><para>
Bir giriş kabuğundan çıkılmasını sağlar. Kabuğun atasına <replaceable>n</replaceable> durumu döner.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-printf">
    <title><literal>printf</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>printf</primary></indexterm>
    <para>
<synopsis><command>printf</command> [<command>-v</command> <varname>değişken</varname>] <emphasis>biçim</emphasis> [<emphasis>argümanlar</emphasis>]
</synopsis>
    </para><para>
<replaceable>biçim</replaceable>'in denetimi altında <replaceable>argümanlar</replaceable>'ı standart çıktıya biçimleyerek yazar. <replaceable>biçim</replaceable> üç tür nesne içeren bir dizgedir: salt karakterler basitçe standart çıktıya kopyalanır, öncelenmiş karakterler dönüştürülüp standart çıktıya kopyalanır ve biçim belirtimleri; belirtimlerin her biri sırayla karşı düşen <replaceable>argümanlar</replaceable>'ın basılmasını sağlar. Standart <command>printf</command>(1) biçimlerine ek olarak, <literal>%b</literal> <command>printf</command>'in tersbölü öncelemeli karakterlerin karşı düşen <replaceable>argüman</replaceable>'da yorumlanmasını sağlar, (çıktıyı sonlandıran <command>\c</command> hariç, <command>\'</command>, <command>\"</command> ve <command>\?</command> içindeki tersbölüler kaldırılmaz ve <command>\0</command> ile başlayan sekizlik öncelemeler dört rakama kadar olabilir) ve <literal>%q</literal> <command>printf</command>'in karşı düşen <replaceable>argüman</replaceable>'ın  kabuk girdisi olarak kullanılabilecek biçimde çıktılamasını sağlar.
    </para><para>
<option>-v</option> seçeneği çıktının standart çıktıya basılması yerine <varname>değişken</varname>'e atanmasını sağlar.
    </para><para>
<replaceable>biçim</replaceable> tüm <replaceable>argümanlar</replaceable> tüketilinceye kadar yeniden kullanılır. <replaceable>argümanlar</replaceable>'ın sayısı yetersizse, kalan biçim belirtimleri için türüne göre sıfır ya da boş dizge kullanılır. Bir hata oluşmazsa dönüş durumu sıfırdır, aksi takdirde sıfırdan farklıdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-read">
    <title><literal>read</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>read</primary></indexterm>
    <para>
<synopsis><command>read</command> [<command>-ers</command>] [<command>-a</command> <emphasis>dizi-ismi</emphasis>] [<command>-d</command> <emphasis>sonlandırıcı</emphasis>] [<command>-n</command> <emphasis>krkt-sayısı</emphasis>] [<command>-p</command> <emphasis>istem</emphasis>]
     [<command>-t</command> <emphasis>zamanaşımı</emphasis>] [<command>-u</command> <emphasis>dosya-tanıtıcı</emphasis>]  [<emphasis>isim</emphasis> &hellip;]
</synopsis>
    </para><para>
Standart girdiden ya da <command>-u</command> seçeneği ile sağlanan <replaceable>dosya-tanıtıcı</replaceable>'dan tek satır okur ve ilk sözcük ilk <replaceable>isim</replaceable>'e, ikinci sözcük ikinci <replaceable>isim</replaceable>'e ve böyle giderek, aradaki sözcükler ve ayraçlar son <replaceable>isim</replaceable>'e kadar atanır. Girdi akımından okunan sözcükler isimlerden daha azsa kalan isimlere boş değerler atanır. <literal>IFS</literal> değişkeninin değerindeki karakterler satırı sözcüklere ayırmakta kullanılır. Tersbölü karakteri (<literal>\</literal>) satırın devam ettirilmesi için ve kendinden sonra gelen özel karakterlerin yorumlanması için kullanılabilir. Hiç isim verilmemişse, satırın tamamı okunur ve <literal>REPLY</literal> değişkenine atanır. Dosyasonu karakteri (<literal>Ctrl-D</literal>) saptanmadıkça, komut zamanaşımına düşmedikçe ya da <command>-u</command> seçeneği ile sağlanan <replaceable>dosya-tanıtıcı</replaceable> geçersiz olmadıkça dönüş durumu sıfırdır. Seçenekler verildiklerinde aşağıdaki anlamlara gelirler:
    </para><glosslist>
<glossentry><glossterm><command>-a </command><replaceable>dizi-ismi</replaceable></glossterm>
<glossdef><para>
Sözcükler sırayla <replaceable>dizi-ismi</replaceable> dizisinin elemanlarına atanır. Dizinin ilk elemanının indisi 0 dır. Atama yapılmadan önce <replaceable>dizi-ismi</replaceable> dizisinin tüm elemanları silinir. Diğer <replaceable>isim</replaceable> argümanları yoksayılır.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-d </command><replaceable>sonlandırıcı</replaceable></glossterm>
<glossdef><para>
<replaceable>sonlandırıcı</replaceable>'nın ilk karakteri girdi satırını  sonlandırmak için satırsonu karakteri yerine kullanılır.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-e</command></glossterm>
<glossdef><para>
Satırı sağlamakta Readline (<xref linkend="bashref_command.line.editing"/> bölümüne bakınız) kullanılır.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-n </command><replaceable>krkt-sayısı</replaceable></glossterm>
<glossdef><para>
Satırın okunması <replaceable>krkt-sayısı</replaceable> karakterde sona erer. Satırın kalanı yoksayılır.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-p </command><replaceable>istem</replaceable></glossterm>
<glossdef><para>
Girdi beklendiğini belirtecek <replaceable>istem</replaceable> dizgesi satırsonu karakteri olmaksızın görüntülenir. İstem sadece girdi bir uçbirimden gelecekse gösterilir.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-r</command></glossterm>
<glossdef><para>
Bu seçenek verildiğinde tersbölü karakteri bir önceleme karakteri olarak ele alınmaz, dolayısıyla tersbölü-satırsonu çifti satırın alt satırda devam edeceğini belirtmekte kullanılamaz.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-s</command></glossterm>
<glossdef><para>
Sessiz kip. Girdi bir uçbirimden geliyorsa karakterler yansılanmaz.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-t </command><replaceable>zamanaşımı</replaceable></glossterm>
<glossdef><para>
Satır <replaceable>zamanaşımı</replaceable> saniye sonra hala sonlandırılmamışsa <command>read</command> zamanaşımına düşer ve hata döner. Bu seçenek, girdi bir uçbirim ya da boruhattından okunmuyorsa etkisizdir.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-u </command><replaceable>dosya-tanıtıcı</replaceable></glossterm>
<glossdef><para>
Girdi<replaceable>dosya-tanıtıcı</replaceable>'dan okunur.
</para></glossdef></glossentry></glosslist>
  </sect1>
  <sect1 xml:id="bashref_set.builtin">
    <title><literal>set</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>set</primary></indexterm>
<para/><para>
<synopsis><command>set</command> [<command>--abefhkmnptuvxBCHP</command>] [<command>-o</command> <emphasis>seçenek</emphasis>] [<emphasis>argüman</emphasis> ...]
</synopsis>
    </para><para>
Seçeneksiz ve argümansız olarak <command>set</command>, tüm kabuk değişkenlerinin ve işlevlerinin isim ve değerlerini yerelin alfabetik sıralamasına göre dizerek, girdi olarak yeniden kullanılabilir biçimde gösterir. Salt-okunur değişkenler sıfırlanamazlar. POSIX kipinde sadece kabuk değişkenleri listelenir.
    </para><para>
Seçenekler verildiğinde kabuk özellikleri olarak atanır ya da kaldırılır. Seçenekler belirtildiğinde aşağıdaki anlamlara gelir:
    </para><glosslist>
<glossentry><glossterm><command>-a</command></glossterm>
<glossdef><para>
Müteakip komutların ortamına aktarılmak üzere değiştirilen veya oluşturulan işlev ve değişkenleri imler.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-b</command></glossterm>
<glossdef><para>
Sonlandırılan artalan işlerin durumunu sonraki birincil komut isteminde gösterilmesini yerine anında raporlanmasını sağlar.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-e</command></glossterm>
<glossdef><para>
Bir <literal>until</literal> veya <literal>while</literal> döngüsünden hemen sonra gelen bir komut listesinin bir kısmında, bir <literal>if</literal> deyiminin sınama parçasında, bir <literal>&amp;&amp;</literal> veya <literal>||</literal> listesinin bir parçasında hata oluşmadıkça veya komutun dönüş durumu <literal>!</literal>  kullanarak ters çevrilmedikçe bir <link linkend="bashref_shell.commands-simple">basit komut</link> sıfırdan farklı bir çıkış durumu ile çıkarsa anında çıkar. <literal>ERR</literal> için bir sinyal kapanı belirtilmişse, kabuk çıkmadan önce çalıştırılır.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-f</command></glossterm>
<glossdef><para>
Dosya ismi üretimini (globbing) iptal eder.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-h</command></glossterm>
<glossdef><para>
Çalıştırmak için aranan komutları bulur ve yerlerini hatırlar (hash) Bu seçenek öntanımlı olarak etkindir.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-k</command></glossterm>
<glossdef><para>
Atama deyimleri şeklindeki tüm argümanları komut isminden önce belirtmek yerine ortama yerleştirir.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-m</command></glossterm>
<glossdef><para>
<link linkend="bashref_job.control">İş denetimi</link> etkinleştirilir.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-n</command></glossterm>
<glossdef><para>
Komutları okur ama onları çalıştırmaz; bu bir betiği sözdizimi
hatalarına karşı denetlemek için yararlıdır. Bu seçenek etkileşimli
kabuklarda yok sayılır.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-o </command><varname>seçenek-ismi</varname></glossterm>
<glossdef><para>
<replaceable>seçenek-ismi</replaceable> olarak aşağıdakilerden biri verildiğinde karşılığı olan seçenekler kullanılmış olur:
</para><glosslist>
<indexterm scope="bashref_shopt.index"><primary>allexport</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-allexport"><option>allexport</option></glossterm>
<glossdef><para>
<option>-a</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>braceexpand</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-braceexpand"><option>braceexpand</option></glossterm>
<glossdef><para>
<option>-B</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>emacs</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-emacs"><option>emacs</option></glossterm>
<glossdef><para>
Bir <literal>emacs</literal> tarzı satır düzenleme arayüzü kullanılır (<xref linkend="bashref_command.line.editing"/> bölümüne bakınız).
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>errexit</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-errexit"><option>errexit</option></glossterm>
<glossdef><para>
<option>-e</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>errtrace</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-errtrace"><option>errtrace</option></glossterm>
<glossdef><para>
<command>-E</command> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>functrace</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-functrace"><option>functrace</option></glossterm>
<glossdef><para>
<command>-T</command> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>hashall</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-hashall"><option>hashall</option></glossterm>
<glossdef><para>
<option>-h</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>histexpand</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-histexpand"><option>histexpand</option></glossterm>
<glossdef><para>
<option>-H</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>history</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-history"><option>history</option></glossterm>
<glossdef><para>
<xref linkend="bashref_bash.history.facilities"/> bölümünde
anlatıldığı gibi komut geçmişini etkinleştirir. Bu seçenek
etkileşimli kabuklarda öntanımlı olarak etkindir.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>ignoreeof</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-ignoreeof"><option>ignoreeof</option></glossterm>
<glossdef><para>
Bir etkileşimli kabuk dosyasonu (EOF) karakterine
rastlanınca çıkmaz.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>keyword</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-keyword"><option>keyword</option></glossterm>
<glossdef><para>
<option>-k</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>monitor</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-monitor"><option>monitor</option></glossterm>
<glossdef><para>
<option>-m</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>noclobber</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-noclobber"><option>noclobber</option></glossterm>
<glossdef><para>
<option>-C</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>noexec</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-noexec"><option>noexec</option></glossterm>
<glossdef><para>
<option>-n</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>noglob</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-noglob"><option>noglob</option></glossterm>
<glossdef><para>
<option>-f</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>nolog</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-nolog"><option>nolog</option></glossterm>
<glossdef><para>
Yoksayılır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>notify</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-notify"><option>notify</option></glossterm>
<glossdef><para>
<option>-b</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>nounset</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-nounset"><option>nounset</option></glossterm>
<glossdef><para>
<option>-u</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>onecmd</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-onecmd"><option>onecmd</option></glossterm>
<glossdef><para>
<option>-t</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>physical</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-physical"><option>physical</option></glossterm>
<glossdef><para>
<option>-P</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>pipefail</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-pipefail"><option>pipefail</option></glossterm>
<glossdef><para>
Etkinse bir boruhattının dönüş değeri ya sıfırdan farklı bir durumla çıkan son (en sağdaki) komutun değeridir ya da boruhattındaki tüm komutlar başarılıysa sıfırdır. Bu seçenek öntanımlı olarak etkin değildir.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>posix</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-posix"><option>posix</option></glossterm>
<glossdef><para>
Standart uyumu için POSIX standardındakinden farklı öntanımlı işlemde Bash davranışını değiştirir (<xref linkend="bashref_posix.mode"/> bölümüne bakınız). Bu, Bash'ın standarda tam uyumunu sağlamak maksadıyla yapılmıştır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>privileged</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-privileged"><option>privileged</option></glossterm>
<glossdef><para>
<option>-p</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>verbose</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-verbose"><option>verbose</option></glossterm>
<glossdef><para>
<option>-v</option> seçeneği ile aynıdır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index"><primary>vi</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-vi"><option>vi</option></glossterm>
<glossdef><para>
Bir <literal>vi</literal> tarzı satır düzenleme arayüzü kullanılır.
</para></glossdef></glossentry>
<indexterm scope="bashref_shopt.index">
<primary>xtrace</primary></indexterm>
<glossentry><glossterm xml:id="bashref_set-xtrace"><option>xtrace</option></glossterm>
<glossdef><para>
<option>-x</option> seçeneği ile aynıdır.
</para></glossdef></glossentry></glosslist>
</glossdef></glossentry>
<glossentry><glossterm><command>-p</command></glossterm>
<glossdef><para>
Ayrıcalıklı kipi etkinleştirir. Bu kipte, <literal>$BASH_ENV</literal> ve <literal>$ENV</literal> dosyaları işlenmez, kabuk işlevleri ortamdan miras alınmaz ve <literal>SHELLOPTS</literal> değişkeni ortamda görünüyorsa yoksayılır. Kabuk, gerçek kullanıcı (grup) kimliği ile aynı olmayan etkin kullanıcı (grup) kimliği ile başlatılmışsa ve <option>-p</option> seçeneği verilmemişse, bu eylemler alınır ve etkin kullanıcı (grup) kimliği, gerçek kullanıcı (grup) kimliğine ayarlanır. Başlatırken <option>-p</option> seçeneği verilmişse, etkin kullanıcı (grup) kimliği sıfırlanmaz. Bu seçeneğin kapatılması etkin kullanıcı ve grup kimliklerinin gerçek kullanıcı ve grup kimliklerine ayarlanmasına sebep olur.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-t</command></glossterm>
<glossdef><para>
Tek bir komutu okuyup çalıştırdıktan sonra çıkar.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-u</command></glossterm>
<glossdef><para>
Parametre yorumlaması uygulanırken bir hata sonucu değişkenlerin kaldırılmasına benzer bir davranış gösterir. Standart hataya bir hata iletisi yazılır ve etkileşimsiz kabuk çıkar.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-v</command></glossterm>
<glossdef><para>
Kabuk girdi satırlarını okundu olarak basar.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-x</command></glossterm>
<glossdef><para>
Basit komutların, <command>for</command>komutlarının, <command>case</command> komutlarının, <command>select</command> komutlarının ve aritmetik <command>for</command> komutlarının ve bunların argümanlarının veya ilişkili sözcük listelerinin yorumlandıktan sonra ve çalıştırılmadan önce izleme listesini basar. <command>PS4</command> değişkeninin değeri yorumlanır ve sonuçlanan değer komuttan ve komutun yorumlanan argümanlarından önce basılır.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-B</command></glossterm>
<glossdef><para>
Kabuk <link linkend="bashref_shell.expansions-brace">kaşlı ayraç yorumlaması</link> uygular. Bu seçenek öntanımlı olarak etkindir.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-C</command></glossterm>
<glossdef><para>
Çıktının <literal>></literal> kullanılarak yönlendirilmesini ve <literal>&lt;></literal> kullanılarak mevcut dosyaların üzerine yazılmasını engeller.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-E</command></glossterm>
<glossdef><para>
Etkinse <command>ERR</command> üstündeki bir tuzak kabuk işlevlerince miras alınır, komut ikameleri ve komutlar bir alt kabuk ortamında çalıştırılır. <command>ERR</command> tuzağı normalde böyle durumlarda miras alınmaz.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-H</command></glossterm>
<glossdef><para>
<literal>!</literal> tarzı <link linkend="bashref_bash.hist.exp">geçmiş ikamesini</link> etkinleştirir. Bu seçenek etkileşimli kabuklarda öntanımlı olarak etkindir.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-P</command></glossterm>
<glossdef><para>
Verildiğinde, örneğin çalışılan dizini değiştirmek için <command>cd</command> gibi bir komut uygulandığında, sembolik bağları izlemez. Yerine fiziksel dizin kullanılır. Öntanımlı olarak, çalışılan dizini değiştiren komutlar uygulandığında, Bash mantıksal dizin zincirlerini izler.
          </para><para>
Örneğin, <filename>/usr/sys</filename> dizini <filename>/usr/local/sys</filename> dizinine bir sembolik bağ ise:
          </para><para>
<screen>$ <command>cd /usr/sys; echo $PWD</command>
/usr/sys
$ <command>cd ..; pwd</command>
/usr
</screen>
          </para><para>
<command>set</command> <option>-P</option> verilirse:
          </para><para>
<screen>$ <command>cd /usr/sys; echo $PWD</command>
/usr/local/sys
$ <command>cd ..; pwd</command>
/usr/local
</screen>
</para></glossdef></glossentry>
<glossentry><glossterm><command>-T</command></glossterm>
<glossdef><para>
Etkinse <command>DEBUG</command> ve <command>RETURN</command> üstündeki bir tuzak kabuk işlevlerince miras alınır, komut ikameleri ve komutlar bir alt kabuk ortamında çalıştırılır. <command>DEBUG</command> ve <command>RETURN</command> tuzakları normalde böyle durumlarda miras alınmaz.
</para></glossdef></glossentry>
<glossentry><glossterm><command>--</command></glossterm>
<glossdef><para>
Bu seçenekten sonra bir argüman verilmezse konumsal parametreler kaldırılır. Aksi takdirde, konumsal parametreler bazıları <literal>-</literal> ile başlasa bile argümanlara ayarlanır.
</para></glossdef></glossentry>
<glossentry><glossterm><command>-</command></glossterm>
<glossdef><para>
Seçeneklerin sonunu belirtir. Kalan tüm argümanlar konumsal parametrelere ayarlanır. <option>-x</option> ve <option>-v</option> seçenekleri kapatılır. Argüman yoksa konumsal parametreler değişmeden kalır.
</para></glossdef></glossentry></glosslist><para>
Seçeneklerdeki <literal>-</literal> işaretleri yerine <literal>+</literal> kullanıldığında bu seçenekler kapatılır. Ayrıca, seçenekler kabuğun çağrılması sırasında da kullanılabilir. Seçeneklerin mevcut listesi <literal>$-</literal> içinde bulunabilir.
    </para><para>
Kalan N <replaceable>argüman</replaceable> konumsal parametrelerdir ve <literal>$1, $2, ... $N</literal> şeklinde atanır. Özel parametre <literal>#</literal> ise N'e ayarlanır.
    </para><para>
Geçersiz seçenek verilmediği sürece dönüş durumu daima sıfırdır.
    </para>
  </sect1>

  <sect1 xml:id="bashref_bash.builtins-shopt">
    <title><literal>shopt</literal> Yerleşiği</title>
    <indexterm scope="bashref_commands.index"><primary>shopt</primary></indexterm>
    <para>
<synopsis><command>shopt</command> [<command>-pqsu</command>] [<command>-o</command>] [<emphasis>seçenek-ismi</emphasis> ...]
</synopsis>
    </para><para>
İsteğe bağlı kabuk davranışlarını kontrol eden değişken değerlerini açıp kapar. Seçeneksiz ya da <option>-p</option> seçeneği ile tüm denetim seçeneklerini etkin olup olmadıklarını belirterek listeler. <option>-p</option> seçeneğinin farkı listlenenlerin kabuğa bir girdi olarak verilebilecek biçemde olmasıdır. Diğer seçeneklerin anlamları:
    </para><variablelist>
<varlistentry><term><literal>-s</literal></term>
<listitem><para>
Belirtilen her <replaceable>seçenek-ismi</replaceable>'ni etkinleştirir (set'in s'si)
</para></listitem></varlistentry>
<varlistentry><term><literal>-u</literal></term>
<listitem><para>
Belirtilen her <replaceable>seçenek-ismi</replaceable>'ni iptal eder (unset'in u'su).
</para></listitem></varlistentry>
<varlistentry><term><literal>-q</literal></term>
<listitem><para>
Normal çıktıyı engeller; <replaceable>seçenek-ismi</replaceable>'nin etkinleştirme sonucunu dönüş durumu belli eder. Belirtilen tüm <replaceable>seçenek-ismi</replaceable>'leri etkinleştirilmişse dönüş durumu sıfırdır, aksi takdirde sıfırdan farklıdır.
</para></listitem></varlistentry>
<varlistentry><term><literal>-o</literal></term>
<listitem><para>
<link linkend="bashref_set.builtin"><command>set</command></link> yerleşiğinin <option>-o</option> seçeneğinde kullanılabilecek <replaceable>seçenek-ismi</replaceable> değerlerini sınırlar.
</para></listitem></varlistentry>
      </variablelist><para>
<option>-s</option> ve <option>-u</option>  seçenekleri birlikte verilemez, bir <replaceable>seçenek-ismi</replaceable> ile birlikte verilmezlerse sadece kendilerine uyan seçenek isimlerini listelerler.
    </para><para>
Aksi belirtilmedikçe, öntanımlı olarak <command>shopt</command> seçenekleri kapalıdır (off).
    </para><para>
Seçenek isimleri listelenirken tüm seçenekler etkinse (on) dönüş durumu sıfırdır, aksi takdirde sıfırdan faklıdır. Seçeneklerin etkinleştirilmesi ve kaldırılması sırasında bir <replaceable>seçenek-ismi</replaceable> geçersiz bir seçenek ismi olmadıkça dönüş durumu sıfırdır.
    </para><para>
<replaceable>seçenek-ismi</replaceable> olarak belirtilebilecek seçenekler:
    </para><variablelist>
<indexterm scope="bashref_shopt.index"><primary>cdable_vars</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-cdable_vars"><option>cdable_vars</option></term>
<listitem><para>
Etkinleştirildiğinde <link linkend="bashref_bourne.shell.builtins-cd"><command>cd</command></link> yerleşiğinin argümanı olarak dizin olmayan bir isim verildiğinde bunun geçilecek dizinin isminin atandığı bir değişkenin ismi olarak kabul edilmesini sağlar.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>cdspell</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-cdspell"><option>cdspell</option></term>
<listitem><para>
Etkinleştirildiğinde, <command>cd</command> komutunda bir dizin isminin yazılışında ufat tefek hatalar varsa düzeltilecektir. Bu hatalar; takdim-tehir, eksik bir karakter ve bir karakterin birden fazla yazılmasıdır. Bir düzeltme uygulanırsa düzeltilen dizin basılır ve komut çalıştırılır. Bu seçenek sadece etkileşimli kabuklarda kullanılır.
</para></listitem></varlistentry>
<indexterm scope="bashref_concept.index"><primary>komut tablosu</primary></indexterm>
<indexterm scope="bashref_shopt.index">
<primary>checkhash</primary></indexterm>
<varlistentry><term xml:id="bashref_bash.builtins-shopt.checkhash"> <option>checkhash</option></term>
<listitem><para>
Etkinleştirildiğinde, Bash, bir komutu çalıştırmadan önce komut tablosuna bakar. Komut tablosunda komutu bulamazsa <literal>PATH</literal> dizinlerine bakar.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>checkwinsize</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-checkwinsize"><option>checkwinsize</option></term>
<listitem><para>
Etkinleştirildiğinde, Bash, her komuttan sonra pencere boyutunu ve lazımsa <literal>LINES</literal> ve <literal>COLUMNS</literal> değerlerindeki değişiklikleri kontrol eder.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>cmdhist</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-cmdhist"><option>cmdhist</option></term>
<listitem><para>
Etkinleştirildiğinde, Bash, aynı geçmiş girdisindeki çok satırlı bir komutun tüm satırlarını kaydetmeye çalışır. Böylece çok satırlı komutların yeniden düzenlenebilmesi kolaylaşır.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>dotglob</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-dotglob"><option>dotglob</option></term>
<listitem><para>
Etkinleştirildiğinde, Bash, dosyaismi yorumlaması sonuçlarına <literal>.</literal> ile başlayan dosyaisimlerini dahil eder.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>execfail</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-execfail"><option>execfail</option></term>
<listitem><para>
Etkinleştirildiğinde, <command>exec</command> yerleşiğine bir argüman olarak verilen bir dosya çalıştırılamadığında bir etkileşimsiz kabuk çıkmaz. Bir etkileşimli kabuk <command>exec</command> başarısız olduğunda zaten çıkmaz.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>expand_aliases</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-expand_aliases"><option>expand_aliases</option></term>
<listitem><para>
Etkinleştirildiğinde, takma adlar <xref linkend="bashref_aliases"/> bölümünde açıklandığı gibi yorumlanır. Bu seçenek etkileşimli kabuklarda öntanımlı olarak etkindir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>extdebug</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-extdebug"><option>extdebug</option></term>
<listitem><para>
Etkinse hata ayıklayıcılar tarafından kullanılmak üzere tasarlanmış davranış etkinleştirilir:
      </para><orderedlist numeration="arabic">
<listitem><para>
<xref linkend="bashref_bash.builtins-declare"/>nin <option>-F</option> seçeneği bir argüman olarak sağlanmış her işleve karşılık düşen kaynak dosya isimlerini ve satır numaralarını gösterir.
</para></listitem><listitem><para>
Eğer komut <command>DEBUG</command> tuzağı tarafından çalıştırılmışsa sıfırdan farklı bir değerle döner, sonraki komut atlanır ve çalıştırılmaz.
</para></listitem><listitem><para>
Eğer komut <command>DEBUG</command> tuzağı tarafından çalıştırılmışsa 2 değeri ile döner ve kabuk bir altyordamı (<command>.</command> veya <command>source</command> yerleşikleri ile çalıştırılan bir kabuk betiği ya da kabuk işlevi) çalıştırarak bir <command>return</command> çağrısını taklit eder.
 </para></listitem><listitem><para>
<command>BASH_ARGC</command> ve <command>BASH_ARGV</command> açıklamalarında belirtildiği gibi güncellenir (bkz, <xref linkend="bashref_shell.variables"/>).
</para></listitem><listitem><para>
İşlev izleme etkinleştirilir:
        </para><para>
<literallayout indent="0">
      ( <varname>komut</varname> )
</literallayout>
        </para><para>
ile çağrılan komut ikamesi, kabuk işlevleri ve alt kabuklar <command>DEBUG</command> ve <command>RETURN</command> tuzaklarını miras alır.
</para></listitem><listitem><para>
Hata izleme etkinleştirilir:
        </para><para>
<literallayout indent="0">
      ( <varname>komut</varname> )
</literallayout>
        </para><para>
ile çağrılan komut ikamesi, kabuk işlevleri ve alt kabuklar <command>ERROR</command> tuzağını miras alır.
</para></listitem></orderedlist>
</listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>extglob</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-extglob"><option>extglob</option></term>
<listitem><para>
Etkinleştirildiğinde, <xref linkend="bashref_shell.expansions-fname.pm"/> bölümünde açıklanan özellikler etkinleştirilir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>extquote</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-extquote"><literal>extquote</literal></term>
<listitem><para>
Etkinse, çit tırnak içine alınmış <literal>${</literal><varname>parametre</varname><literal>}</literal> genişletmesi içinde <literal>$'</literal><varname>dizge</varname><literal></literal> ve <literal>$"</literal><varname>dizge</varname><literal>"</literal> biçimi tırnak içine alma uygulanır. Bu seçenek öntanımlı olarak etkindir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>failglob</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-failglob"><literal>failglob</literal></term>
<listitem><para>
Etkinse dosyayolu yorumlaması sırasında dosya isimleri kalıplarla eşleşmezse sonuç bir yorumlama hatasıdır.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>force_fignore</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-force_fignore"><literal>force_fignore</literal></term>
<listitem><para>
Etkinse <link linkend="bashref-FIGNORE"><literal>FIGNORE</literal></link> kabuk değişkeni tarafından belirtilen sonekler sözcük tamamlama uygulanırken yoksayılan sözcükler tek olası tamamlamalar olsalar bile sözcüklerin yoksayılmalarına sebep olurlar. Bu seçenek öntanımlı olarak etkindir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>gnu_errfmt</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-gnu_errfmt"><literal>gnu_errfmt</literal></term>
<listitem><para>
Etkinse kabuk hata iletileri standart GNU hata iletileri biçiminde yazılır.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>histappend</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-histappend"><option>histappend</option></term>
<listitem><para>
Kabuk çıkarken normalde geçmiş listesini, ismi <literal>HISTFILE</literal> değişkeninin değerinden alınan dosyanın üzerine yazar. Seçenek etkinleştirildiğinde geçmiş listesi bu dosyaya eklenir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>histreedit</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-histreedit"><option>histreedit</option></term>
<listitem><para>
Etkinleştirildiğinde,  Readline kullanımda ise, bir başarısız geçmiş ikamesini düzenlemesi için kullanıcıya bir fırsat tanınır.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>histverify</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-histverify"><option>histverify</option></term>
<listitem><para>
Etkinleştirildiğinde,  Readline kullanımda ise geçmiş ikamesi kabuk çözümleyiciye hemen aktarılmaz. Bunun yerine, değişikliklerin yapılabilmesi için sonuç satırı Readline düzenleme tamponuna yüklenir.
      </para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>hostcomplete</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-hostcomplete"><option>hostcomplete</option></term>
<listitem><para>
Etkinleştirildiğinde,  Readline kullanımda ise, Bash, <literal>@</literal> içeren bir sözcüğe konakismi tamamlaması uygulamaya çalışır (<xref linkend="bashref_rl.commands-rltype"/> bölümüne bakınız). Bu seçenek öntanımlı olarak etkindir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>huponexit</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-huponexit"><option>huponexit</option></term>
<listitem><para>
Etkinleştirildiğinde, bir etkileşimli giriş kabuğu çıktığında Bash, tüm işlere <literal>SIGHUP</literal> sinyali gönderecektir (<xref linkend="bashref_shell.exec-signals"/> bölümüne bakınız).
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>interactive_comments</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-interactive_comments"><option>interactive_comments</option></term>
<listitem><para>
Etkileşimli kabuklarda <literal>#</literal> ile başlayan bir sözcük ve devamındaki karakterlerin yoksayılması sağlanır. Bu seçenek öntanımlı olarak  etkindir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>lithist</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-lithist"><option>lithist</option></term>
<listitem><para>
Etkinleştirildiğinde, <option>cmdhist</option> seçeneği de etkinse, çok satırlı komutların satırları mümkün olduğunca <literal>;</literal> karakterleri ile ayrılarak değil gömülü satırsonları korunarak kaydedilir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>login_shell</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-login_shell"><option>login_shell</option></term>
<listitem><para>
Kabuk, bir giriş kabuğu olarak başlatılırsa bu seçeneği etkinleştirir (<xref linkend="bashref_invoking.bash"/> bölümüne bakınız). Ancak değer değiştirilmeyebilir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>mailwarn</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-mailwarn"><option>mailwarn</option></term>
<listitem><para>
Etkinleştirildiğinde, Bash'in eposta denetimi için kullandığı bir dosyaya son denetlemeden sonra erişildiğinde "<emphasis><literal>eposta-dosyası</literal> içindeki eposta okundu</emphasis>" iletisini gösterir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>no_empty_cmd_completion</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-no_empty_cmd_completion"><option>no_empty_cmd_completion</option></term>
<listitem><para>
Etkinleştirildiğinde,  Readline kullanımda ise, bir boş satır tamamlanmaya çalışıldığında mümkün tamamlamaları bulmak için Bash, <literal>PATH</literal> dizinlerinde arama başlatmayacaktır.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>nocaseglob</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-nocaseglob"><option>nocaseglob</option></term>
<listitem><para>
Etkinleştirildiğinde, dosyaismi yorumlaması uygulanırken harf büyüklüğüne duyarsız dosyaismi eşlemesi yapılır.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>nocasematch</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-nocasematch"><literal>nocasematch</literal></term>
<listitem><para>
Etkinse <command>case</command> veya <command>[[</command> koşullu komutları çalıştırılırken eşleşme uygulandığında Bash kalıpları harf büyüklüğüne duyarsız kalarak eşleştirir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>nullglob</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-nullglob"><option>nullglob</option></term>
<listitem><para>
Etkinleştirildiğinde, Bash, kendisine hiçbir dosya uymayan dosyaismi kalıplarının kendileri yerine boş dizge döndürmelerine izin verir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>progcomp</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-progcomp"><option>progcomp</option></term>
<listitem><para>
Etkinleştirildiğinde, <link linkend="bashref_programmable">programlanabilir tamamlama yetenekleri</link> etkinleştirilir. Bu seçenek öntanımlı olarak etkindir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>promptvars</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-promptvars"><option>promptvars</option></term>
<listitem><para>
Etkinleştirildiğinde, <link linkend="bashref_prompts">komut istemi</link> dizgeleri yorumlandıktan sonra bir de parametre yorumlaması, komut ikamesi, aritmetik yorumlaması ve tırnak kaldırmaya maruz kalır. Bu seçenek öntanımlı olarak etkindir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>restricted_shell</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-restricted_shell"> <option>restricted_shell</option></term>
<listitem><para>
Kabuk bu seçeneği, <link linkend="bashref_restricted.shell">sınırlı kipte</link> başlatıldığında etkinleştirir. Değeri değişmeyebilir. Bash, başlatma dosyaları çalıştırılırken, başlangıç dosyalarının kabuğun sınırlı kipte olup olmadığını keşfetmesini sağlayarak sıfırlanmamasını sağlar.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>shift_verbose</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-shift_verbose"><option>shift_verbose</option></term>
<listitem><para>
Etkinleştirildiğinde, konumsal parametrelerin kaydırma miktarı parametre sayısını aşarsa <link
linkend="bashref_bourne.shell.builtins-shift"><command>shift</command> yerleşiği</link> bir hata iletisi basar.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>sourcepath</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-sourcepath"><option>sourcepath</option></term>
<listitem><para>
Etkinleştirildiğinde, <link linkend="bashref_bash.builtins-source"><command>source</command> yerleşiğine</link> argüman olarak verilen bir dosyanın yerini bulmak için <literal>PATH</literal> değeri kullanılır. Bu seçenek öntanımlı olarak etkindir.
</para></listitem></varlistentry>
<indexterm scope="bashref_shopt.index"><primary>xpg_echo</primary></indexterm>
<varlistentry><term xml:id="bashref_shopt-xpg_echo"><option>xpg_echo</option></term><listitem><para>
Etkinleştirildiğinde, <link linkend="bashref_bash.builtins-echo"><command>echo</command> yerleşiği</link> öntanımlı olarak tersbölü öncelemeli karakterleri yorumlar.
</para></listitem></varlistentry></variablelist><para>
Tüm <replaceable>seçenek-ismi</replaceable>'leri etkinse, seçenekler listelendiğinde dönüş durumu sıfırdır, aksi takdirde sıfırdan farklıdır. Seçenekleri etkinleştirirken ya da kaldırırken bir <replaceable>seçenek-ismi</replaceable> bir geçersiz kabuk seçeneği olmadıkça dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-source">
    <title><literal>source</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>source</primary></indexterm>
    <para>
<synopsis><command>source</command> <emphasis>dosyaismi</emphasis>
</synopsis>
    </para><para>
<link linkend="bashref_bourne.shell.builtins-period"><command>.</command> (nokta) yerleşiği</link> ile eşdeğerdir.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-type">
    <title><literal>type</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>type</primary></indexterm>
    <para>
<synopsis><command>type</command> [<command>-afptP</command>] [<emphasis>isim</emphasis> &hellip;]
</synopsis>
    </para><para>
Her <replaceable>isim</replaceable>'in nasıl yorumlanması gerektiğini belirtir. Örneğin, <command>type type</command> komutu <emphasis>type is a shell builtin</emphasis> çıktılar (anlamı: "type bir kabuk yerleşiğidir").
    </para><para>
<option>-t</option> seçeneği verildiğinde, <replaceable>isim</replaceable> bir takma ad ise <literal>alias</literal>, bir işlev ise <literal>function</literal>, bir yerleşik komut ise <literal>builtin</literal>, bir disk dosyası ise <literal>file</literal> veya bir anahtar sözcük ise  <literal>keyword</literal> sözcüğünü basar. <replaceable>isim</replaceable> bulunamazsa, hiçbir şey basılmaz ve <command>type</command> bir başarısızlık durumu ile döner.
    </para><para>
<option>-p</option> seçeneği verildiğinde, <replaceable>isim</replaceable> <option>-t</option> seçeneği ile <literal>file</literal> çıktısını vermiyorsa hiçbir şey dönmez, aksi takdirde çalıştırılacak disk dosyasının ismi basılır.
    </para><para>
<command>-P</command> seçeneği her <replaceable>isim</replaceable> için, <command>-t</command> dosya döndürmese bile bir yol araması yapılmasını zorlar.
    </para><para>
Komut çırpılanmışsa <command>-p</command> ve <command>-P</command> çırpılı değeri basar, dosyanın ilk olarak <literal>$PATH</literal> içinde görünmesi gerekli değildir.
    </para><para>
<option>-a</option> seçeneği verildiğinde, <replaceable>isim</replaceable> çalıştırılabilir dosyasının bulunabileceği yerleri basar. Bu, sadece ve sadece <option>-p</option> seçeneği kullanılmamışsa takma adları ve işlevleri de içerir.
    </para><para>
<option>-f</option> seçeneği belirtilmişse <command>command</command> yerleşiğindeki gibi <command>type</command> kabuk işlevlerini bulmaya çalışmaz.
    </para><para>
<replaceable>isim</replaceable>lerden biri bulunursa dönüş durumu sıfırdır, hiçbiri bulunamazsa sıfırdan farklıdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-typeset">
    <title><literal>typeset</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>typeset</primary></indexterm>
    <para>
<synopsis><command>typeset</command> [<command>-afFrxi</command>] [<command>-p</command>] [<emphasis>isim</emphasis>[=<emphasis>değer</emphasis>] &hellip;]
</synopsis>
    </para><para>
<command>typeset</command> komutu Korn kabuğu ile uyumluluk için vardır. Yerine <link linkend="bashref_bash.builtins-declare"><command>declare</command> yerleşiğinin</link> kullanılması önerilir.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-ulimit">
    <title><literal>ulimit</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>ulimit</primary></indexterm>
    <para>
<synopsis><command>ulimit</command> [<command>-acdefilmnpqrstuvxSH</command>] [<emphasis>sınır</emphasis>]
</synopsis>
    </para><para>
<command>ulimit</command> kabuk tarafından başlatılan süreçlerin kullanabildiği özkaynaklar üzerinde, sistem buna izin veriyorsa, denetim sağlar. Seçenekler verildiğinde şu anlamlara gelir:
    </para><variablelist>
<varlistentry><term><option>-S</option></term>
<listitem><para>
Bir özkaynakla ilişkili yazılımsal sınırı (soft limit) raporlar ya da değiştirir.
</para></listitem></varlistentry>
<varlistentry><term><option>-H</option></term>
<listitem><para>
Bir özkaynakla ilişkili donanımsal sınırı (hard limit) raporlar ya da değiştirir.
</para></listitem></varlistentry>
<varlistentry><term><option>-a</option></term>
<listitem><para>
Mevcut tüm sınırlamalar gösterilir.
</para></listitem></varlistentry>
<varlistentry><term><option>-c</option></term>
<listitem><para>
Oluşan <filename>core</filename> dosyalarının azami boyu (core file size).
</para></listitem></varlistentry>
<varlistentry><term><option>-d</option></term>
<listitem><para>
Bir sürecin veri segmanının azami boyu (data seg size).
</para></listitem></varlistentry>
<varlistentry><term><option>-e</option></term>
<listitem><para>
Azami zamanlama önceliği (nice).
</para></listitem></varlistentry>
<varlistentry><term><option>-f</option></term>
<listitem><para>
Kabuk ve çocukları tarafından yazılan dosyaların azami boyu (file size).
</para></listitem></varlistentry>
<varlistentry><term><option>-i</option></term>
<listitem><para>
Askıdaki sinyallerin azami sayısı.
</para></listitem></varlistentry>
<varlistentry><term><option>-l</option></term>
<listitem><para>
Belleğe kilitlenebilen azami uzunluk (max locked memory).
</para></listitem></varlistentry>
<varlistentry><term><option>-m</option></term>
<listitem><para>
Azami bellek boyu (max memory size).
</para></listitem></varlistentry>
<varlistentry><term><option>-n</option></term>
<listitem><para>
Açık dosya tanıtıcılarının azami sayısı (open files).
</para></listitem></varlistentry>
<varlistentry><term><option>-p</option></term>
<listitem><para>
Boruhattı tamponunun boyu (pipe size).
</para></listitem></varlistentry>
<varlistentry><term><option>-q</option></term>
<listitem><para>
POSIX ileti kuyruğunun azami bayt sayısı.
</para></listitem></varlistentry>
<varlistentry><term><option>-r</option></term>
<listitem><para>
Azami anında çalıştırma zamanlaması önceliği.
</para></listitem></varlistentry>
<varlistentry><term><option>-s</option></term>
<listitem><para>
Yığının azami boyu (stack size).
</para></listitem></varlistentry>
<varlistentry><term><option>-t</option></term>
<listitem><para>
İşlemci zamanının saniye cinsinden azami miktarı (cpu time).
</para></listitem></varlistentry>
<varlistentry><term><option>-u</option></term>
<listitem><para>
Tek bir kullanıcının kullanabileceği azami süreç sayısı (max user processes).
</para></listitem></varlistentry>
<varlistentry><term><option>-v</option></term>
<listitem><para>
Bir sürecin kullanabileceği sanal belleğin azami miktarı (virtual memory).
</para></listitem></varlistentry>
<varlistentry><term><option>-x</option></term>
<listitem><para>
Dosya kilitlerinin azami sayısı.
</para></listitem></varlistentry>
      </variablelist><para>
<replaceable>sınır</replaceable> verilmişse, belirtilen özkaynağın yeni değeridir; özel <replaceable>sınır</replaceable> değerleri mevcut donanımsal sınır için <literal>hard</literal>, mevcut yazılımsal sınır için <literal>soft</literal> ve sınırsız için <literal>unlimited</literal>'dir. Aksi takdirde, <option>-H</option> seçeneği belirtilmedikçe belirtilen özkaynak için mevcut yazılımsal sınır gösterilir. Yeni sınırlar ayarlanırken ne <option>-H</option> ne de <option>-S</option> seçeneği verilmişse hem yazılımsal hem de donanımsal sınırlar ayarlanır. Hiç seçenek verilmezse <option>-f</option> seçeneği verilmiş kabul edilir. Değerler <option>-t</option> için saniye cinsinden, <option>-p</option> için 512 baytlık blokların sayısı olarak, <option>-n</option> ve <option>-u</option> için birimsiz, kalan seçenekleri için 1024 baytlık blokların sayısı olarak belirtilmelidir.
    </para><para>
Bir geçersiz seçenek ya da değer belirtilmedikçe veya yeni bir sınır belirtilirken bir hata oluşmadıkça dönüş durumu sıfırdır.
    </para>
  </sect1>
  <sect1 xml:id="bashref_bash.builtins-unalias">
    <title><literal>unalias</literal> Yerleşiği</title>
<indexterm scope="bashref_commands.index"><primary>unalias</primary></indexterm>
    <para>
<synopsis><command>unalias</command> [<command>-a</command>] [<emphasis>isim</emphasis> ... ]
</synopsis>
    </para><para>
Belirtilen her <replaceable>isim</replaceable> takma adlar listesinden kaldırılır. <option>-a</option> seçeneği verilirse, tüm takma adlar kaldırılır. Takma adlar <xref linkend="bashref_aliases"/> bölümünde açıklanmıştır.
    </para>
  </sect1>
</chapter>

  <chapter xml:id="bashref_special.builtins" label="3">
    <title>Özel Yerleşikler</title>
    <titleabbrev>POSIX tarafından özel olarak sınıflanmış yerleşik komutlar.</titleabbrev>
<indexterm scope="bashref_concept.index">
<primary>yerleşik komutlar</primary><secondary>POSIX'e özel</secondary></indexterm>
<indexterm scope="bashref_concept.index">
<primary>özel</primary><secondary>yerleşik komut</secondary></indexterm>
    <para>
Tarihsel sebeplerle, POSIX standardında <emphasis>özel</emphasis> olarak sınıflandırılmış yerleşik komutlar vardır. Bash, POSIX kipinde çalıştırılırken, özel yerleşikler diğer yerleşiklerden üç hususta ayrılır:
    </para><orderedlist>
        <listitem>
Komut aranırken özel yerleşiklere kabuk işlevlerinden önce bakılır.
        </listitem><listitem>
Bir özel yerleşik bir hata durumu ile dönerse, bir etkileşimsiz kabuk çıkar.
        </listitem><listitem>
Komutttan önce verilen atama deyimleri komut işini tamamladıktan sonra da kabukta etkin olarak kalır.
        </listitem>
      </orderedlist><para>
Bash'ın POSIX kipinde çalıştırılmaması durumunda, bu yerleşikler
Bash yerleşik komutlarından farklı davranmaz. Bash POSIX kipi <xref
linkend="bashref_posix.mode"/> bölümünde anlatılmıştır.
    </para><para>
POSIX'e özel yerleşikler:
    </para><para>
<literallayout indent="0">
    break : . continue eval exec exit export readonly return set
    shift trap unset
</literallayout>
    </para>
  </chapter>
</part>
